server {

    listen 80;

    server_name globalpresence.co.in www.globalpresence.co.in;

    return 301 https://$host$request_uri;
}


server {

    listen 443 ssl;

    server_name globalpresence.co.in www.globalpresence.co.in;

    ssl_certificate /etc/letsencrypt/live/globalpresence.co.in/fullchain.pem;

    ssl_certificate_key /etc/letsencrypt/live/globalpresence.co.in/privkey.pem;

    client_max_body_size 20M;

    location /static/ {

        alias usr/share/nginx/html/static/;

        expires 30d;

        add_header Cache-Control "public, max-age=2592000";

        access_log off;
    }

    location / {

        proxy_pass http://flask_app:2423;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;
    }
}